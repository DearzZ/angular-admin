{
  "name": "jimujia-boss",
  "version": "5.0.0",
  "description": "",
  "scripts": {
    "dev": "npm install && rm -rf ../public && grunt build:dev && cp -R www/ ../public && cp -R src/* ../public/",
    "dist": "npm install && rm -rf ../public && grunt build:dist && cp -R www/* dist/ && rm -rf ../public && mv dist ../public "
  },
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-concat": "^0.5.0",
    "grunt-contrib-connect": "^1.0.2",
    "grunt-contrib-copy": "^0.5.0",
    "grunt-contrib-htmlmin": "^0.3.0",
    "grunt-contrib-imagemin": "^1.0.1",
    "grunt-contrib-jshint": "^1.1.0",
    "grunt-contrib-uglify": "^0.5.1",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-recess": "^1.0.0",
    "gruntfile-gtx": "^0.3.0",
    "imagemin-mozjpeg": "^6.0.0",
    "require-directory": "^2.0.0"
  }
}
